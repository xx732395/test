name: Bump version

on:
  workflow_dispatch:  

jobs:
  bump-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Commit changes
        run: |
          git branch
          echo 123 >> 1.txt
          git config --global user.email "421826878@qq.com"
          git config --global user.name "xx732395"
          git add .
          git commit -m "Bump version to $NEW_VERSION"
          git push origin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

 
